// Rest Parameters
function printParams(params1, ...params) {
  let sum = 0;
  for (let param of params) {
    sum += param;
  }
  console.log(sum);
}

printParams(1, 1, 2, 3);

// Using Returned Value as Parameter

// Call Back Function

// Arrow Syntax - consise body

// Arrow Syntax - Block Body
